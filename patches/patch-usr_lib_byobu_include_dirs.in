--- a/usr/lib/byobu/include/dirs.in	2026-03-11 01:59:03.746789290 -0400
+++ b/usr/lib/byobu/include/dirs.in	2026-03-11 02:19:43.286329742 -0400
@@ -44,7 +44,7 @@
 [ -r "$BYOBU_CONFIG_DIR/socketdir" ] && . "$BYOBU_CONFIG_DIR/socketdir"
 
 # Create and export the runtime cache directory
-if [ -w /dev/shm ]; then
+if [ -d /dev/shm ] && [ -w /dev/shm ]; then
 	# Use shm for performance, if possible
 	for i in /dev/shm/$PKG-$USER-*; do
 		if [ -d "$i" ] && [ -O "$i" ]; then
